
/*! Placeholder html5-qrcode build.
 * Replace this file with the real library for camera scanning.
 * Until then, provide a tiny shim so the UI does not crash.
 */
(function(g){
  function ScannerShim(el, cfg){ this._el = el; }
  ScannerShim.prototype.render = function(onScan){ this._on = onScan; var self=this; setTimeout(function(){},10); };
  ScannerShim.prototype.clear = function(){ return Promise.resolve(); };
  g.Html5QrcodeScanner = ScannerShim;
})(window);
